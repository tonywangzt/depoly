package myapp;
use Dancer ':syntax';
our $VERSION = '0.1';

#before sub {
#   if (!session('user') && request->path_info !~m{/login}){
#       redirect '/login';
#       #var requestd_path => request->path_info;
#       #request->path_info('/login');
#   }
#};







true;
